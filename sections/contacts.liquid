<!-- Contacts section content (Shopify section fragment) -->

			<main class="max-w-4xl mx-auto px-6 py-24">
				<header class="text-center mb-12">
					<h1 class="text-4xl md:text-6xl font-black italic">Get in Touch</h1>
					<p class="text-gray-400 mt-4">We're happy to help — questions about products, orders or partnerships.</p>
				</header>

				<section class="grid grid-cols-1 md:grid-cols-2 gap-8">
					<div class="space-y-6">
						<div>
							<h3 class="text-xl font-bold">Contact Information</h3>
							<p class="text-gray-400 mt-2 text-sm">Choose the channel that's most convenient for you.</p>
						</div>

						<ul class="space-y-3 text-sm">
							<li><strong>Email:</strong> <a href="mailto:cozyspot.team@gmail.com" class="text-purple-400 hover:underline">cozyspot.team@gmail.com</a></li>
							<li><strong>Hours:</strong> Mon–Sun, 09:00–21:00 (UTC)</li>
						</ul>

						<div class="mt-6 text-sm text-gray-500">
							<p>Warehouse & returns address is available in <a href="shipping-policy.html" class="text-white/80 hover:underline">Shipping Policy</a>.</p>
						</div>
					</div>

					<div>
						<div class="bg-[#0b0b0b] border border-white/5 rounded-2xl p-6">
							<form id="contact-form" class="space-y-4" action="mailto:cozyspot.team@gmail.com" method="post" enctype="text/plain">
								<div>
									<label class="block text-[12px] mb-1">Name</label>
									<input name="name" required class="w-full bg-transparent border border-white/10 rounded px-3 py-2 text-sm" />
								</div>

								<div>
									<label class="block text-[12px] mb-1">Email</label>
									<input type="email" name="email" required class="w-full bg-transparent border border-white/10 rounded px-3 py-2 text-sm" />
								</div>

								<div>
									<label class="block text-[12px] mb-1">Message</label>
									<textarea name="message" rows="5" required class="w-full bg-transparent border border-white/10 rounded px-3 py-2 text-sm"></textarea>
								</div>

								<div class="flex items-center justify-between">
									<button type="submit" class="bg-purple-600 px-4 py-2 rounded font-bold uppercase text-[12px]">Send</button>
									<button type="reset" class="text-white/60 text-sm">Clear</button>
								</div>
							</form>

							<p class="text-[11px] text-white/40 mt-3">If the form doesn't send, email us directly at cozyspot.team@gmail.com</p>
						</div>
					</div>
				</section>
			</main>

{% schema %}
{
	"name": "Contacts",
	"settings": [],
	"blocks": [],
	"presets": [
		{ "name": "Contacts", "category": "Pages" }
	]
}
{% endschema %}
